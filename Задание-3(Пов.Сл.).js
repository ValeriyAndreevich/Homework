// let start = +prompt("Введите первое число последовательности");
  // let finish = +prompt("Введите последнее число последовательности");

  // let arr = Array.from({length: finish - start + 1}, (_, a) => a + start);
  // console.log(arr)


  // let sum=0;
  // let a=prompt("");
  // let arr=[a];
  // while (a!=null) {
  //     a=prompt("");
  //     arr.push(+a);
  // }
  // for (let i = 0; i < (arr.length-1); i++ ) {
  //   sum += +arr[i];
  // }
  // alert (sum);
